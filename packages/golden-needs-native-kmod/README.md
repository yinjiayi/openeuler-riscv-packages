# golden-needs-native-kmod

This package requires building, loading, and observing a kernel module on a native RISC-V kernel. QEMU user-mode CI must return `needs-native-riscv`, must not invent a patch, and must not enable auto-merge.
