<!-- SPDX-License-Identifier: Apache-2.0 -->
# mpz

This directory packages upstream `https://github.com/olegantonyan/mpz` version `2.1.2` for openEuler 24.03 LTS SP3 on `riscv64`/RVA23.

Exact-head Package CI run `33670333195` completed dependency preparation in 447 seconds, then compiled under QEMU for the remaining 2,853 seconds and reached 50% before the 60-minute package budget expired. The build log contained no concrete compiler or test failure. Release 2 therefore raises only the bounded package timeout to 180 minutes; the complete build and `%check` remain enabled and unchanged.

External source and patch licenses remain those of their respective upstream projects. The repository license only covers original packaging metadata, scripts, and documentation.
