<!-- SPDX-License-Identifier: Apache-2.0 -->
# gambit

This directory packages upstream `https://github.com/gambitproject/gambit` version `16.7.0` for openEuler 24.03 LTS SP3 on `riscv64`/RVA23.

Exact-head Package CI run `33671406005` completed dependency preparation in 233 seconds, then compiled under QEMU for the remaining 3,067 seconds and logged 61 distinct C++ object compilation commands before the 60-minute package budget expired. The build log contained no concrete compiler or test failure, and `%check` had not started. Release 2 therefore raises only the bounded package timeout to 180 minutes; the complete build and `%check` remain enabled and unchanged.

External source and patch licenses remain those of their respective upstream projects. The repository license only covers original packaging metadata, scripts, and documentation.
