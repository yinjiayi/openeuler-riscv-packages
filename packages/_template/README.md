# Package template

Copy this directory through `scripts/create-package`; do not edit it into a second package in place. Replace every placeholder, pin official source digests, add the smallest meaningful smoke test, and record every patch in `package.yaml`, `patches/series`, and the SPEC.
