interface:
  display_name: "Linux Package Discovery"
  short_description: "Discover and normalize stable upstream source packages"
  default_prompt: "Use $linux-package-discovery to discover trusted stable source-package candidates."
