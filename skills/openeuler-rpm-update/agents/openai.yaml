interface:
  display_name: "openEuler RPM Update"
  short_description: "Check and update managed RPMs without duplicate PRs"
  default_prompt: "Use $openeuler-rpm-update to check managed packages for stable releases."
